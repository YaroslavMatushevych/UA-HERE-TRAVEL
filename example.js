{/* <script type="text/javascript" src="<?php echo get_template_directory_uri(); ?>/js/modernizr-custom.js"></script>
Modernizr.on('webp', function (result) {
  if (result) {
        $("[data-webp]").each(function(i,elem) {
            $(elem).css('background-image', 'url(' + $(elem).attr("data-webp") + ')');
        });
  }
  else {
        $("[data-img]").each(function(i,elem) {
            $(elem).css('background-image', 'url(' + $(elem).attr("data-img") + ')');
        });
  }
}); */}